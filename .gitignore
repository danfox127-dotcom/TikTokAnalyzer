# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
venv/
env/
.venv/
.env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data files — never commit user exports
*.json
!package.json
!package-lock.json
!tsconfig.json
!pyrightconfig.json
!mock-data.json
# Committed golden fixtures for the engine parity harness (WP-1.1)
!algorithmic-mirror/engine/__fixtures__/*.json
data/*
# Committed reference data (not user exports): the TikTok ad-targeting taxonomy
!data/tiktok-ad-taxonomy.json
# Committed corpus calibration: aggregate statistics only, no row-level data.
# See docs/corpus-calibration.md.
!data/corpus_calibration.json
!data/stopwatch_calibration.json
exports/
# Claude Code scratch worktrees. One of these was committed as a gitlink (mode
# 160000) with no .gitmodules, which made `git submodule` operations fail with
# exit 128 — visible as a warning in every CI run.
.claude/worktrees/
user_data_tiktok.json
instagram-export*/

# Reports
report.json

# Local secrets: ignore .env itself, but keep the committed template
.env
!.env.example

# superpowers scratch (SDD progress, brainstorm mockups)
.superpowers/
