# ⚠️ FROZEN — legacy engine, superseded by the WP-1.1 port

This directory is the original, incomplete TypeScript prototype of the forensics
engine. **Do not extend it.** Under Gate 0 (TypeScript-everywhere), the engine is
being re-ported module-by-module against the frozen Python oracle
(`api/ghost_profile.py`, `parsers/tiktok.py`, `utils/*.py`) with a golden-fixture
parity harness.

- **New canonical port:** `algorithmic-mirror/engine/` (currently: `stopwatch.ts`,
  `parseDate.ts`, `videoId.ts`, plus `__tests__/stopwatch.parity.test.ts`). This is
  a temporary home; it promotes to a shared engine package once the WP-1.1 layout
  is settled.
- **Oracle:** the Python engine + its `tests/` suite (139 passing) is the behavioral
  source of truth. Parity fixtures are generated by
  `scripts/gen_stopwatch_parity_fixture.py` and committed.

This copy stays only as historical reference until the port reaches parity, then it
is deleted. Any change made here will silently drift from both the oracle and the
new port — hence: frozen.
